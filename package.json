{
  "name": "ethereumjs-lib-bridge",
  "version": "0.0.1",
  "description": "ethereumjs lib bridge",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browserify index.js -t [ babelify --global true ] --standalone bridge > ethereumjs-lib-app.js"
  },
  "author": "bitbill",
  "dependencies": {
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-minify": "^0.2.0",
    "babelify": "^8.0.0",
    "browserify": "^14.5.0",
    "ethereumjs-util": "^4.5.0",
    "ethereumjs-wallet": "^0.6.0",
    "bip39": "^2.4.0"
  }
}
