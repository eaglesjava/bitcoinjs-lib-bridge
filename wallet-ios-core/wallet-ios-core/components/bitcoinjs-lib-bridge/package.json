{
  "name": "bitcoinjs-lib-bridge",
  "version": "0.0.1",
  "description": "bitcoinjs lib bridge",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browserify index.js -t [ babelify --global true ] --standalone bridge > bitcoinjs-lib-app.js"
  },
  "author": "bitbill",
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-minify": "^0.2.0",
    "babelify": "^8.0.0",
    "bip32-utils": "^0.11.1",
    "bip39": "^2.4.0",
    "bitcoinjs-lib": "^3.3.1",
    "browserify": "^14.5.0"
  }
}
